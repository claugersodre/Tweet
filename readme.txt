- Setup
to run the project, in the root of project run the command docker-compose up --build -d.
the backend will start on http://localhost:3001
to stop application, in the root of project run the command docker-compose down

- Critique
 - Database may be a problem because it has a max number of connections.
 - How this project was made with docker containers it was made to fit scaling.